{% for post in posts %}
	<a data-el-ajax-single-blogpost="{{post.nid}}" href="http:/{{post.path}}" data-toggle="modal" data-target="#modal-nid-{{post.nid}}">
		<h2> {{ post.title }} </h2>
	</a>
	<div> {{ post.body }} </div>
{% endfor %}